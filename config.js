// Replace this file with your own API key and settings
const API_KEY = 'd164afa7cba94e5e87906ec03643623b';

// Export the key if you want to keep it in a separate file
if (typeof module !== 'undefined') {
    module.exports = {
        API_KEY
    };
}
